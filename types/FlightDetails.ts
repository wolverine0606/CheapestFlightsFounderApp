export type FlightDetails = {
  from: {
    airline: string;
    arriveAt: string;
    departAt: string;
    duration: string;
  };
  price: string;
  to: {
    airline: string;
    arriveAt: string;
    departAt: string;
    duration: string;
  };
};
